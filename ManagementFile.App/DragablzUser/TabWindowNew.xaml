<Window x:Class="ManagementFile.App.DragablzUser.TabWindowNew"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:dragablz="clr-namespace:Dragablz;assembly=Dragablz"
        xmlns:local="clr-namespace:ManagementFile.App.DragablzUser"
        mc:Ignorable="d"
        Title="TabWindowNew" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TabablzControl - QUAN TRỌNG: phải có x:Name="TabControl" -->
        <dragablz:TabablzControl x:Name="TabControl"
                                Grid.Row="1"
                                ItemsSource="{Binding Tabs}"
                                SelectedItem="{Binding SelectedTab, Mode=TwoWay}"
                                HeaderItemTemplate="{StaticResource TabHeaderTemplate}"
                                ContentTemplate="{StaticResource TabContentTemplate}"
                                Margin="10"
                                ShowDefaultCloseButton="False"
                                Background="Transparent">

            <dragablz:TabablzControl.InterTabController>
                <dragablz:InterTabController 
                    InterTabClient="{Binding InterTabClient}"
                    Partition="ProjectManagementTabs"/>
            </dragablz:TabablzControl.InterTabController>

            <dragablz:TabablzControl.CustomHeaderItemStyle>
                <Style TargetType="{x:Type dragablz:DragablzItem}" 
                       BasedOn="{StaticResource CustomDragablzItemStyle}">
                </Style>
            </dragablz:TabablzControl.CustomHeaderItemStyle>

        </dragablz:TabablzControl>
    </Grid>
</Window>
