<Window x:Class="ManagementFile.App.Views.Project.CommentDeletionReasonDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Xóa bình luận - Nhập lý do" 
        Height="600" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8F9FA">

    <Window.Resources>
        <!-- Button Styles -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3498DB"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980B9"/>
                                <Setter Property="BorderBrush" Value="#2980B9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#21618C"/>
                                <Setter Property="BorderBrush" Value="#21618C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDC3C7"/>
                                <Setter Property="BorderBrush" Value="#BDC3C7"/>
                                <Setter Property="Foreground" Value="#7F8C8D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SmallButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="MinWidth" Value="60"/>
            <Setter Property="Height" Value="26"/>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#95A5A6"/>
            <Setter Property="BorderBrush" Value="#95A5A6"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#7F8C8D"/>
                    <Setter Property="BorderBrush" Value="#7F8C8D"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#566573"/>
                    <Setter Property="BorderBrush" Value="#566573"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#E74C3C"/>
            <Setter Property="BorderBrush" Value="#E74C3C"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C0392B"/>
                    <Setter Property="BorderBrush" Value="#C0392B"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#A93226"/>
                    <Setter Property="BorderBrush" Value="#A93226"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#3498DB"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#5DADE2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern TextBlock Style -->
        <Style x:Key="ModernTextBlock" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>

        <Style x:Key="HeaderTextBlock" TargetType="TextBlock" BasedOn="{StaticResource ModernTextBlock}">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="LabelTextBlock" TargetType="TextBlock" BasedOn="{StaticResource ModernTextBlock}">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#34495E"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="🗑️ Xóa bình luận" 
                       Style="{StaticResource HeaderTextBlock}"
                       Foreground="#E74C3C"/>
            <TextBlock Text="Vui lòng nhập lý do xóa bình luận này để ghi nhận vào lịch sử." 
                       FontSize="12" 
                       Foreground="#7F8C8D"
                       TextWrapping="Wrap"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Comment Info -->
        <Border Grid.Row="1" 
                Background="White"
                BorderBrush="#E1E4E8"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12"
                Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Thông tin bình luận sẽ xóa:" 
                           Style="{StaticResource LabelTextBlock}"
                           Margin="0,0,0,5"/>
                <TextBlock Text="{Binding CommentInfo}" 
                           FontSize="11"
                           Foreground="#586069"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Reason Input -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Lý do xóa *" 
                       Style="{StaticResource LabelTextBlock}"
                       Margin="0,0,0,8"/>
            <TextBox x:Name="ReasonTextBox"
                     Text="{Binding DeletionReason, UpdateSourceTrigger=PropertyChanged}"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     Style="{StaticResource ModernTextBox}"
                     Height="80"
                     VerticalContentAlignment="Top"/>

            <!-- Predefined reasons -->
            <StackPanel Margin="0,10,0,0">
                <TextBlock Text="Lý do thường gặp:" 
                           FontSize="11" 
                           Foreground="#7F8C8D"
                           Margin="0,0,0,5"/>
                <WrapPanel>
                    <Button Content="Không còn cần thiết" 
                            Click="PredefinedReason_Click"
                            Style="{StaticResource SmallButton}"
                            Background="Transparent"
                            Foreground="#495057"
                            BorderBrush="#BDC3C7"
                            Margin="0,0,5,5"/>
                    <Button Content="Thông tin sai" 
                            Click="PredefinedReason_Click"
                            Style="{StaticResource SmallButton}"
                            Background="Transparent"
                            Foreground="#495057"
                            BorderBrush="#BDC3C7"
                            Margin="0,0,5,5"/>
                    <Button Content="Trùng lặp" 
                            Click="PredefinedReason_Click"
                            Style="{StaticResource SmallButton}"
                            Background="Transparent"
                            Foreground="#495057"
                            BorderBrush="#BDC3C7"
                            Margin="0,0,5,5"/>
                    <Button Content="Vi phạm quy định" 
                            Click="PredefinedReason_Click"
                            Style="{StaticResource SmallButton}"
                            Background="Transparent"
                            Foreground="#495057"
                            BorderBrush="#BDC3C7"
                            Margin="0,0,5,5"/>
                    <Button Content="Spam" 
                            Click="PredefinedReason_Click"
                            Style="{StaticResource SmallButton}"
                            Background="Transparent"
                            Foreground="#495057"
                            BorderBrush="#BDC3C7"
                            Margin="0,0,5,5"/>
                    <Button Content="Nội dung không phù hợp" 
                            Click="PredefinedReason_Click"
                            Style="{StaticResource SmallButton}"
                            Background="Transparent"
                            Foreground="#495057"
                            BorderBrush="#BDC3C7"
                            Margin="0,0,5,5"/>
                </WrapPanel>
            </StackPanel>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="✅ Xác nhận xóa" 
                    Name="ConfirmButton"
                    Click="ConfirmButton_Click"
                    Style="{StaticResource DangerButton}"
                    MinWidth="120"
                    Margin="0,0,10,0"/>
            <Button Content="❌ Hủy bỏ" 
                    IsCancel="True"
                    Style="{StaticResource SecondaryButton}"
                    MinWidth="80"/>
        </StackPanel>
    </Grid>
</Window>